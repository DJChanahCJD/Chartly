{
  "name": "chartly",
  "version": "0.1.0",
  "private": true,
  "description": "Chartly — free public music charts & awards API",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250810.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
